apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Define o namespace para todos os recursos
namespace: selecoes-dev

resources:
- ../../base
- namespace.yaml

patches:
- path: patch-deployment.yaml
- path: patch-ingress.yaml
- path: patch-hpa.yaml

# MERGE com valores específicos de DEVELOPMENT
configMapGenerator:
  - name: selecoes-config
    behavior: merge
    options:
      disableNameSuffixHash: true
    literals:
      - LOG_LEVEL="debug"
      - MESSAGE="Serviço Seleções Development"

images:
- name: dti-registro.unilab.edu.br/unilab/selecoes
  newTag: dev
