apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: selecoes
spec:
  rules:
    - host: selecoes-staging.unilab.edu.br  # Host de staging
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: selecoes
                port:
                  number: 80
